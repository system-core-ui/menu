/** Default placement for dropdown menu content */
export const DEFAULT_PLACEMENT = 'bottom-start' as const;

/** Default placement for popover sub-menu */
export const DEFAULT_SUB_PLACEMENT = 'right-start' as const;

/** Default offset from trigger (px) */
export const DEFAULT_OFFSET = 4;

/** Default sub-menu offset (px) */
export const DEFAULT_SUB_OFFSET = 0;

/** Default max height before scrolling */
export const DEFAULT_MAX_HEIGHT = 300;

/** Default min width */
export const DEFAULT_MIN_WIDTH = 180;

/** Default z-index (matches Popover) */
export const DEFAULT_Z_INDEX = 1400;

/** Hover open delay for sub-menu (ms) */
export const HOVER_OPEN_DELAY = 150;

/** Hover close delay for sub-menu (ms) */
export const HOVER_CLOSE_DELAY = 300;
